<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= content_for(:title) || "BKEKI - BABY KEKIUS" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
        body {
            margin: 0;
            font-family: 'Comic Sans MS', 'Comic Sans', cursive;
            background: linear-gradient(to bottom, #7daa29, #008000);
            color: #333;
            text-align: center;
            line-height: 1.6;
        }

        .hero-section {
            background-color: #7daa29;
            color: white;
            padding: 50px 20px;
            position: relative;
        }

        .hero-section h1 {
            font-size: 3rem;
            font-weight: bold;
            margin: 20px 0;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .hero-section h3 {
            font-size: 1.5rem;
            font-weight: bold;
            margin: 20px 0;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .hero-section img {
            border-radius: 50%;
            width: 150px;
            height: 150px;
            border: 4px solid white;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
            margin: 20px auto;
        }

        .section {
            margin: 40px auto;
            padding: 20px;
            max-width: 800px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
        }

        .section h2 {
            font-size: 2rem;
            color: #008000;
            text-transform: uppercase;
            margin-bottom: 10px;
        }

        .section p {
            font-size: 1.2rem;
            color: #444;
            text-align: justify;
            margin: 10px 0;
        }

        .btn-group {
            margin-top: 20px;
        }

        .btn {
            display: inline-block;
            padding: 10px 20px;
            margin: 10px 5px;
            color: white;
            text-decoration: none;
            background: linear-gradient(to right, #ff5733, #c70039);
            border-radius: 20px;
            font-size: 1rem;
            font-weight: bold;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.5);
        }

        .list {
            text-align: left;
            margin: 20px auto;
            padding: 0 20px;
        }

        .list li {
            margin: 10px 0;
            font-size: 1.1rem;
        }

        .list li strong {
          color: #008000;
        }

        .likes-dislikes ul {
            list-style-type: none;
            padding: 0 20px;
        }

        .likes-dislikes ul li {
            margin: 10px 0;
        }
        .likes-dislikes ul > li {
            font-size: 1.5rem;
            font-weight: bold;
            color: #f8b400;
            margin-top: 20px;
            text-transform: uppercase;
        }
        .likes-dislikes ul > li.green {
            color: #008000;
        }

        .likes-dislikes ul ul li {
            font-size: 1rem;
            color: #444;
            text-align: left;
            margin: 5px 0;
            font-weight: normal;
            text-transform: none;
            list-style: disc;
        }
        .likes-dislikes ul ul li strong{
          color: #f8b400;
        }
        .likes-dislikes ul ul.green li strong{
          color: #008000;
        }
    </style>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

<body>
    <%= yield %>
    <!-- Join Section -->
    <div class="section">
        <h2>Join the BKEKI Legion</h2>
        <div class="btn-group">
            <a href="https://www.snek.fun/token/1f7638d861bbf70b742643e562a19ec8c6bdc04211d6b10fc80ea45a.42414259204b454b4955532043544f" class="btn">Buy BKEKI</a>
            <a href="https://discord.com/invite/X7qgDyyU" class="btn">Join Community</a>
        </div>
    </div>
</body>
</html>
